import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'center',
    paddingHorizontal: 8,
    paddingBottom: 8,
    paddingTop: 16,
    zIndex: -1,
  },
  button: {
    height: 48,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
